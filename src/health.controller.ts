import { Controller, Get, ServiceUnavailableException } from '@nestjs/common'; import { DataSource } from 'typeorm'; @Controller('health') export class HealthController {constructor(private ds:DataSource){} @Get() async health(){try{await this.ds.query('SELECT 1');return {status:'ok',database:'ok'};}catch{throw new ServiceUnavailableException({message:'Base de datos no disponible',errorCode:'DATABASE_UNAVAILABLE'});}}}
