import 'reflect-metadata'; import * as dotenv from 'dotenv'; import * as bcrypt from 'bcrypt'; import dataSource from './data-source'; import { BusinessConfig, Category, Permission, Role, User } from './entities'; dotenv.config();
const permissions=['productos.ver','productos.crear','productos.editar','productos.eliminar','inventario.ver','inventario.entrada','inventario.ajuste','ventas.ver','ventas.crear','ventas.cancelar','clientes.ver','clientes.crear','clientes.editar','promociones.ver','promociones.crear','promociones.editar','dashboard.ver','usuarios.ver','usuarios.crear','usuarios.editar','bitacora.ver','configuracion.editar'];
async function seed(){if(!process.env.SEED_ADMIN_PASSWORD)throw new Error('SEED_ADMIN_PASSWORD es obligatorio para ejecutar el seed');await dataSource.initialize();const roles=dataSource.getRepository(Role),perms=dataSource.getRepository(Permission),users=dataSource.getRepository(User),categories=dataSource.getRepository(Category),configs=dataSource.getRepository(BusinessConfig);let admin=await roles.findOne({where:{nombre:'ADMIN'},relations:{permissions:true}});if(!admin)admin=await roles.save(roles.create({nombre:'ADMIN',descripcion:'Administración total'}));const saved=[] as Permission[];for(const clave of permissions){let p=await perms.findOne({where:{clave}});if(!p)p=await perms.save(perms.create({clave,nombre:clave.replace('.',' '),modulo:clave.split('.')[0]}));saved.push(p);}admin.permissions=saved;await roles.save(admin);const username=process.env.SEED_ADMIN_USERNAME??'admin';if(!await users.exists({where:{username}}))await users.save(users.create({nombre:'Administrador ChuyDex',username,passwordHash:await bcrypt.hash(process.env.SEED_ADMIN_PASSWORD,Number(process.env.BCRYPT_ROUNDS??12)),roleId:admin.id,activo:true}));for(const [nombre,icono,orden] of [['Galletas','cookie',1],['Papitas','chips',2],['Chocolates','chocolate',3],['Dulces','candy',4],['Panecitos','cake',5],['Bebidas','drink',6],['Otros','box',7]] as [string,string,number][])if(!await categories.exists({where:{nombre}}))await categories.save(categories.create({nombre,icono,orden}));if(!await configs.exists({where:{}}))await configs.save(configs.create({nombreNegocio:'ChuyDex',slogan:'¡Atrápalos antes de que se acaben!',moneda:'MXN',zonaHoraria:'America/Hermosillo'}));await dataSource.destroy();} seed().catch(e=>{console.error(e);process.exit(1)});
