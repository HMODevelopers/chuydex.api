import { Body, Controller, Get, Put, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { BusinessConfig } from '../database/entities'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { Permissions } from '../common/decorators/permissions.decorator'; import { ConfigDto } from './settings.dto';
@ApiTags('configuración') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('configuracion') export class SettingsController {constructor(@InjectRepository(BusinessConfig)private repo:Repository<BusinessConfig>){} @Get() @Permissions('dashboard.ver') async get(){return (await this.repo.findOne({where:{}}))??this.repo.save(this.repo.create({}));} @Put() @Permissions('configuracion.editar') async update(@Body()d:ConfigDto){const c=(await this.repo.findOne({where:{}}))??this.repo.create({});Object.assign(c,d);return this.repo.save(c);} }
