import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { TenantModule } from '../common/tenant/tenant.module'; import { Product, Promotion, Reward, Sale } from '../database/entities'; import { DashboardController } from './dashboard.controller'; @Module({imports:[TypeOrmModule.forFeature([Sale,Product,Promotion,Reward]),TenantModule],controllers:[DashboardController]}) export class DashboardModule {}
