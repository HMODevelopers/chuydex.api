import { Body, Controller, Get, Param, ParseIntPipe, Patch, Post, Query, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PlatformAdminGuard } from '../common/guards/platform-admin.guard'; import { PaginationDto } from '../common/dto/pagination.dto'; import { CreateBusinessOwnerDto, UpdateBusinessStatusDto } from './businesses.dto'; import { BusinessesService } from './businesses.service';
@ApiTags('administración de plataforma') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PlatformAdminGuard) @Controller('plataforma/negocios') export class BusinessesController {constructor(private s:BusinessesService){}@Get()list(@Query()q:PaginationDto){return this.s.list(q)}@Get(':id')one(@Param('id',ParseIntPipe)id:number){return this.s.one(id)}@Post()create(@Body()b:CreateBusinessOwnerDto){return this.s.create(b)}@Patch(':id/estatus')status(@Param('id',ParseIntPipe)id:number,@Body()b:UpdateBusinessStatusDto){return this.s.status(id,b.estatus)}}
