import { Body, Controller, Get, Param, ParseIntPipe, Patch, Post, Put, Query, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { Permissions } from '../common/decorators/permissions.decorator'; import { PaginationDto } from '../common/dto/pagination.dto'; import { CreateUserDto, UpdateUserDto } from './users.dto'; import { UsersService } from './users.service';
@ApiTags('usuarios') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('usuarios') export class UsersController {constructor(private s:UsersService){} @Get() @Permissions('usuarios.ver') list(@Query()q:PaginationDto){return this.s.list(q)} @Get(':id') @Permissions('usuarios.ver') one(@Param('id',ParseIntPipe)id:number){return this.s.one(id)} @Post() @Permissions('usuarios.crear') create(@Body()b:CreateUserDto){return this.s.create(b)} @Put(':id') @Permissions('usuarios.editar') update(@Param('id',ParseIntPipe)id:number,@Body()b:UpdateUserDto){return this.s.update(id,b)} @Patch(':id/estatus') @Permissions('usuarios.editar') status(@Param('id',ParseIntPipe)id:number,@Body('activo')a:boolean){return this.s.status(id,a)} }
