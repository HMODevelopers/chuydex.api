import { Controller, Get, Query, UseGuards } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { AuditLog } from '../database/entities'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { Permissions } from '../common/decorators/permissions.decorator'; import { PaginationDto } from '../common/dto/pagination.dto';
@ApiTags('bitácora') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('bitacora') export class AuditController {constructor(@InjectRepository(AuditLog)private repo:Repository<AuditLog>){} @Get() @Permissions('bitacora.ver') async list(@Query()q:PaginationDto,@Query('modulo')modulo?:string,@Query('accion')accion?:string){const where:any={};if(modulo)where.modulo=modulo;if(accion)where.accion=accion;const[data,total]=await this.repo.findAndCount({where,relations:{user:true},order:{createdAt:'DESC'},skip:(q.page-1)*q.limit,take:q.limit});return {data,meta:{page:q.page,limit:q.limit,total,totalPages:Math.ceil(total/q.limit)}}} }
