import { Controller, Get, UseGuards } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { Permission, Role } from '../database/entities'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { Permissions } from '../common/decorators/permissions.decorator';
@ApiTags('roles y permisos') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller() export class RolesController {constructor(@InjectRepository(Role)private roles:Repository<Role>,@InjectRepository(Permission)private permissions:Repository<Permission>){} @Get('roles') @Permissions('usuarios.ver') listRoles(){return this.roles.find({relations:{permissions:true}})} @Get('permisos') @Permissions('usuarios.ver') listPermissions(){return this.permissions.find({order:{modulo:'ASC',clave:'ASC'}})} }
