import { Body, Controller, Get, Param, ParseIntPipe, Post, Query, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { Permissions } from '../common/decorators/permissions.decorator'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { PaginationDto } from '../common/dto/pagination.dto'; import { CreateSaleDto } from './sales.dto'; import { SalesService } from './sales.service';
@ApiTags('ventas') @ApiBearerAuth() @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('ventas') export class SalesController {constructor(private s:SalesService){} @Get() @Permissions('ventas.ver') list(@Query()q:PaginationDto){return this.s.list(q)} @Get(':id') @Permissions('ventas.ver') one(@Param('id',ParseIntPipe)id:number){return this.s.findOne(id)} @Post() @Permissions('ventas.crear') create(@Body()b:CreateSaleDto,@CurrentUser()u:{id:number}){return this.s.create(b,u.id)} @Post(':id/cancelar') @Permissions('ventas.cancelar') cancel(@Param('id',ParseIntPipe)id:number,@CurrentUser()u:{id:number}){return this.s.cancel(id,u.id)} }
