import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { Business, BusinessConfig, Product, Promotion } from '../database/entities'; import { PublicController } from './public.controller'; @Module({imports:[TypeOrmModule.forFeature([Business,Product,Promotion,BusinessConfig])],controllers:[PublicController]}) export class PublicModule {}
